# First Prototype Workflow

> **Config:** Resolve `{FORK_DIR}`, `{EDIT_MARKER}`, `{FORK_NAME}` from `.agents/fork-config.md`.

1. Find a similar local prototype.
2. Choose a parent.
3. Add only needed components.
4. Add localization.
5. Add assets with attribution if needed.
6. Run resource validation.

For {FORK_NAME}, place the prototype under `{FORK_DIR}`.
