# SS14-ART-CORE Preflight Checklist

> **Note:** Resolve `{FORK_DIR}`, `{EDIT_MARKER}`, `{FORK_NAME}` from `.agents/fork-config.md` before using this checklist.

Before coding:

- Read root `AGENTS.md`.
- Read relevant subtree `AGENTS.md`.
- Load `ss14-art-core`, `ss14-ai-workflow`, and domain skills.
- Search for existing mechanics.
- Confirm new code can live under `{FORK_DIR}`.
- Confirm no `RobustToolbox` edit is needed.
- Plan prediction, localization, validation, and review notes.
